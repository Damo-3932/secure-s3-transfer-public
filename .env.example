# Copy this file to .env (or .env.user) and fill in your values.
# These values are local-only and should NOT be committed.
# Tip: Most values can be pulled from Terraform outputs or AWS console.

# ------------------------------------------------------------------------------
# AWS SSO (Identity Center)
# ------------------------------------------------------------------------------
# Where to find these values:
# - Start URL: AWS console → IAM Identity Center → Settings → Start URL
# - Region: the region where Identity Center is configured
# - Account ID: your AWS account ID
# - Role names: permission set names from Terraform (default shown below)
SSO_START_URL=https://YOUR_START_URL.awsapps.com/start
SSO_REGION=ap-southeast-2
SSO_ACCOUNT_ID=<AWS_ACCOUNT_ID>
SSO_UPLOAD_ROLE_NAME=SecureS3Transfer-Uploader
SSO_DOWNLOAD_ROLE_NAME=SecureS3Transfer-Downloader

# ------------------------------------------------------------------------------
# Defaults for CLI profiles
# ------------------------------------------------------------------------------
# AWS_REGION is used by scripts and config generation.
# AWS_OUTPUT should stay "json" to keep parsing consistent.
AWS_REGION=ap-southeast-2
AWS_OUTPUT=json

# ------------------------------------------------------------------------------
# Client config (required for setup-client.ps1)
# ------------------------------------------------------------------------------
# BUCKET_NAME: The transfer bucket name created by Terraform.
# Example: prod-secure-s3-transfer-001
BUCKET_NAME=<S3_TRANSFER_BUCKET_NAME>

# KMS_KEY_ID: Full ARN of the CMK used for the transfer bucket.
# Find it in Terraform outputs or AWS KMS → Customer managed keys.
KMS_KEY_ID=arn:aws:kms:<AWS_REGION>:<AWS_ACCOUNT_ID>:key/<KMS_KEY_ID>

# Prefixes should match Terraform defaults unless you intentionally changed them.
INCOMING_PREFIX=incoming/
ARTEFACTS_PREFIX=incoming/artefacts/
DOWNLOADED_PREFIX=downloaded/

# Transfer Acceleration (true/false). Must be enabled on the bucket to work.
# If false, uploads use standard endpoints.
ENABLE_TRANSFER_ACCELERATION=true
